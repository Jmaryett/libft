#include "libft.h"

char    *ft_strnstr (const char *str1, const char *str2, size_t len)
{
    char *s1 = (char*)str1; char *s2 = (char*)str2; size_t i = 0;
    if (!str1 || !len)
        return (NULL);
    if (str2 == '\0')
        return (s1);
    while (i < len)
    {
        
        i++;
    }
    
    return (NULL);
}